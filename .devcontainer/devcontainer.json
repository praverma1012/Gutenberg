{
  "name": "Flutter Codespace (MCR)",
  "image": "mcr.microsoft.com/devcontainers/flutter:3.22",   // ← public, no auth
  "postCreateCommand": [
    "flutter doctor -v",
    "flutter config --enable-web",
    "cd /workspaces/Gutenberg && flutter pub get"
  ]
}
